<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.97.5/css/materialize.min.css">
        <script type="text/javascript" src="https://code.jquery.com/jquery-2.1.1.min.js"></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.97.5/js/materialize.min.js"></script>
        <title>Stylists | Fadeaway Salon</title>
    </head>
    <body>
        <div class="container">
            <h1>Fadeaway Salon</h1>
<!-- START STYLIST LIST -->
            {% if stylists is not empty %}
                <h1>Here are all of your stylists:</h1>
                <ul>
                    {% for stylist in stylists %}
                        <li><a href="/stylist/{{ stylist.getId }}">{{ stylist.getStylistName }}</a> --- <a href="tel:+1{{  stylist.getStylistPhoneNumber }}"> {{ stylist.getStylistPhoneNumber }}</a></li>
                    {% endfor %}
                </ul>
            {% else %}
                <h3>You haven't added any stylists yet! Use the form below to get started!</h3>
            {% endif %}
<!-- END STYLIST LIST -->
<!-- START ADD STYLIST FORM -->
            <div class="row">
                <form class="col s12" action="/add_stylist" method="post">
                    <div class="row">
                        <div class="input-field col s6">
                            <label for="name">Stylist's Name</label>
                            <input name="name" type="text" class="validate">
                        </div>
                        <div class="input-field col s6">
                            <label for="phone_number">Stylist's Phone Number</label>
                            <input name="phone_number" type="tel" class="validate">
                        </div>
                    </div>
                    <button class="btn waves-effect waves-light" type="submit" name="action">Submit</button>
                </form>
            </div>
<!-- END FORM -->
        </div>
    </body>
</html>
