<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.97.5/css/materialize.min.css">
        <script type="text/javascript" src="https://code.jquery.com/jquery-2.1.1.min.js"></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.97.5/js/materialize.min.js"></script>
        <title>{{ stylist.getStylistName }}</title>
    </head>
    <body>
        <div class="container">
<!-- START CLIENT LIST -->
            <h2>{{ stylist.getStylistName }}</h2>

            {% if clients is not empty %}
                <h4>Here are all of {{ stylist.getStylistName }}'s clients:</h4>
                    <ul>
                        {% for client in clients %}
                            <li><a href="/client_edit">{{ client.getClientName }}</a> --- <a href="tel:+1{{ client.getClientPhoneNumber }}">{{ client.getClientPhoneNumber }}</a></li>
                        {% endfor %}
                    </ul>
            {% else %}
                <h4>{{ stylist.getStylistName }} currently doesn't have any clients listed! Use the form below to add a client for {{ stylist.getStylistName }}</h4>
            {% endif %}
<!-- END CLIENT LIST -->
<!-- START ADD CLIENT FORM -->
            <div class="row">
                <form class="col s12" action="/add_client" method="post">
                    <input type="hidden" name="stylist_id" value="{{ stylist.getId }}">
                    <div class="row">
                        <div class="input-field col s6">
                            <label for="name">Client's Name</label>
                            <input name="name" type="text" class="validate" required>
                        </div>
                        <div class="input-field col s6">
                            <label for="phone_number">Client's Phone Number</label>
                            <input name="phone_number" type="tel" class="validate" required>
                        </div>
                    </div>
                    <button class="btn waves-effect waves-light" type="submit" name="action">Submit</button>
                </form>
            </div>
<!-- END FORM -->
            <p><a href="/">Click here</a> to go back to your homepage or <a href="/stylist/{{ stylist.getId }}/edit">click here</a> to edit or delete {{ stylist.getStylistName }}'s info.</p>
        </div>
    </body>
</html>
